# ~/.gitconfig_win
#
# global git configuration overrides for Windows/ADO
# (set in each repo's .git/config to override these)

[core]
    autocrlf = true

[credential]
    helper = manager

[diff]
    external = difft --skip-unchanged --display=inline
