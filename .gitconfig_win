# ~/.gitconfig_win
#
# global git configuration overrides for Windows
# (set in each repo's .git/config to override these)

[core]
    autocrlf = true

[credential]
    helper = manager

[diff]
    external = difft --skip-unchanged --display=inline
